import React from "react";
import { storiesOf } from "@storybook/react";
import AppToolbar from "./AppToolbar";

storiesOf("Components/App Toolbar", module).add("Redux Container", () => (
  <AppToolbar />
));
